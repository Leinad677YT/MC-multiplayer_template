## NOT PLAYED SURVIVAL => RETURN
    $execute if data storage z_p$(lower):root data.mode.survival run return fail
##

## SET DEFAULT
    $function l.user:player/set_mode_to_default {lower:$(lower),mode:"survival"}
##
